{
  "name": "muse-nexus-witness",
  "version": "0.1.0",
  "private": true,
  "description": "Muse Nexus Witness: quietly keeps the real things people say and do for you, and brings one back on the days you choose.",
  "license": "MIT",
  "type": "module",
  "workspaces": ["packages/*", "apps/core", "apps/web"],
  "scripts": {
    "setup:dev": "bun scripts/setup-dev.mjs",
    "dev": "bun scripts/setup-dev.mjs --check && bun run --filter @witness/web build && bun run --filter @witness/core dev",
    "build": "bun run --filter @witness/web build",
    "test": "bun run --filter '*' test",
    "typecheck": "bun run --filter '*' typecheck",
    "check": "bun run typecheck && bun run test && bun run build",
    "e2e": "bun scripts/e2e.mjs",
    "shortcuts": "bun scripts/shortcuts/build.mjs"
  },
  "engines": { "node": ">=22" },
  "packageManager": "bun@1.2.23"
}
